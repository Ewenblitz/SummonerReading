<section class = "comment">
  <blockquote>
    <%= comment.commentary %>
  </blockquote>
  <p class = "small text-right">
    Par <%= comment.user.username %> le <%= l(comment.created_at.to_date, format: "%d/%m/%Y") %>
  </p>
</section>
